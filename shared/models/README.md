# Shared Data Models

Common data models and schemas used across office services.

## Models

- `Office` - Base office entity
- `OfficeManager` - Manager agent for each office
- `Request` - Inter-office communication request
- `Response` - Standard response format
